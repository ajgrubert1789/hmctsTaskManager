HMCTS Task Management System  


Overview  

This project provides:

- A Java Spring Boot backend exposing a REST API for task management  
- A React frontend offering a clean, user‑friendly interface  
- Full CRUD functionality  
- Validation on both frontend and backend  
- API documentation via Swagger  
- Unit tests using Mockito  
- H2 in‑memory database for development and testing  

The system is designed to be simple, readable, and aligned with typical HMCTS service patterns.

Features

Backend
- Create tasks with:
  - TaskID
  - Title  
  - Description  
  - Status  
  - Due date and time  
Users can: 
- Retrieve a task by ID  
- Retrieve all tasks  
- Update task status, description and title   
- Update due date/time  
- Delete tasks  
- Automatic movement of overdue tasks into the “Completed” section  
- Validation:
  - Title required  
  - Description required  
  - Due date must be in the future  
  - Due time must be in the future  
- Structured error responses  
- Unit tests using Mockito  
- Swagger UI for API documentation  

Frontend
-React GUI
- Create new tasks  
- Update existing tasks (only when a valid ID is provided)  
- Delete tasks using a bin/trash icon  
- Mark tasks complete/incomplete using a circular toggle icon  
- Search tasks by ID  
- View completed tasks on a dedicated page  
- All pages are accessible via navigation tabs  
- Validation preventing invalid submissions  
- Overdue tasks automatically marked as completed  
- Clean, readable UI designed around basic CRUD operations  



Tech Stack

Backend
- Java 
- Spring Boot  
- Spring Web  
- Spring Data JPA  
- H2 Database  
- Mockito / JUnit  
- Swagger / OpenAPI  

Frontend
- React (Hooks)  
- Axios  
- Vite or Create React App (depending on your setup)  
- CSS modules or standard CSS  


Backend
```bash
cd backend
./gradlew bootRun
```

Swagger UI will be available at:  
`http://localhost:8082/swagger-ui.html`

Frontend
```bash
cd frontend
npm install
npm run dev
```


Database  
The backend uses an **H2 in‑memory database** for development and testing.  
Schema is automatically generated by JPA.

You can access the H2 console at:  
`http://localhost:8082/h2-console`

API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/tasks` | Create a new task |
| GET | `/tasks/{id}` | Retrieve a task by ID |
| GET | `/tasks` | Retrieve all tasks |
| PATCH | `/tasks/{id}/status` | Update task status |
| PATCH | `/tasks/{id}/due-date` | Update due date/time |
| DELETE | `/tasks/{id}` | Delete a task |

Full documentation available via Swagger.

---

Testing  
Backend tests use JUnit + Mockito:

- Service logic  
- Validation behaviour  
- Repository interactions  
- Custom Error handling  

Run tests with:

```bash
./gradlew test
```

Frontend UI Overview  

The UI is designed to be simple, readable, and intuitive:

- Task List Page – view all active tasks  
- Create Task Page – add new tasks with validation  
- Update Task Page – update tasks by entering a valid ID  
- Search Page – search tasks by ID  
- Completed Page – view tasks marked as completed or overdue  
- Task Card Icons:
  - Circular icon : toggle complete/incomplete  
  - Trash icon : delete task  

All pages are accessible via the tab navigation bar.


Validation & Error Handling  
Both frontend and backend enforce:

- Required fields  
- Future due date/time  
- Prevention of invalid updates  
- Meaningful error messages  
- Warnings when attempting to update a non‑existent task  

This ensures data integrity and a smoother user experience.


Future Improvements  

If given more time, the following enhancements would be added:

- Persistent database  
- Pagination and sorting  
- Filtering by status or due date  
- Accessibility improvements
- Integration tests  
- Responsiveness 
- Security Features 
- modify frontend to be more modular (using React components) 